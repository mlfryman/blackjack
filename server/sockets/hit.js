// (function(){
//   'use strict';
//
//   angular.module('blackjack')
//     .controller('Ctrl', ['$rootScope', '$scope', '$state', 'User', function($rootScope, $scope, $state, User){
//
//
//   }]);
// })();


// Iron is a Hapi module that decrypts cookies
// Iron.unseal decrypts cookie

//- this selects & matches the right cookie
// cookie = cookie.match(/hapi-cookie=([^;]*)/)[1];

//- .filter is a regular JS array method; called it var player b/c knows will return one player once function runs
//- cannot hit anymore once array gets to [0], b/s [0] = false
//- take card from deck; & push that card into your array of cards
